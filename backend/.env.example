# ============================================
# CONFIGURACIÓN DE ENTORNO
# ============================================
# Copie este archivo a .env y configure los valores

# Entorno de ejecución
# Valores: development, production, test
NODE_ENV=development

# Puerto del servidor
PORT=3001

# ============================================
# BASE DE DATOS (PostgreSQL)
# ============================================
# Formato: postgresql://usuario:password@host:puerto/nombre_db
DATABASE_URL=postgresql://postgres:TU_PASSWORD@localhost:5432/TU_BASE_DE_DATOS
# Producción (Render/Cloud) recomendado:
# DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db?sslmode=verify-full
# Si necesitas compatibilidad libpq explícita:
# DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db?uselibpqcompat=true&sslmode=require

# ============================================
# SEGURIDAD
# ============================================
# Secreto para firmar tokens JWT
# IMPORTANTE: En producción, usar un valor aleatorio largo (mínimo 32 caracteres)
# Puede generar uno con: openssl rand -base64 64
JWT_SECRET=CAMBIE_ESTE_VALOR_POR_UN_SECRETO_SEGURO_EN_PRODUCCION


# ============================================
# NOTAS
# ============================================
# - NUNCA commitear el archivo .env con secretos reales
# - El archivo .env está en .gitignore
# - DATABASE_URL debe apuntar a PostgreSQL (no SQLite)
# - Para conexiones SSL en nube, usar sslmode=verify-full para mantener semántica segura explícita
